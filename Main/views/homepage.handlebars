{{#each Products as |Product| }}
<div class="row col-sm-6">
  <div class="col-sm-4 ml-1">
    <h3>{{Product.name}}</h3>
  </div>
  <div class="col-sm-2 ml-1 ">
    <h3>{{Product.price}}</h3>
  </div>
</div>
{{/each}}

